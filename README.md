# devops-dashboard
Practice repo for Devops course

This program has three modules:

1) Sysinfo, which takes the basic system info and prints it (hostname, username, cwd)
2) Logcheck, which accepts a filename and then returns every line with "error" in it.
3) Tasklist, which is a basic to;do feature.
